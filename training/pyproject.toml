[project]
name = "poker-training"
version = "0.1.0"
description = "OpenSpiel CFR training utilities for poker bots"
requires-python = ">=3.10"
dependencies = [
    "absl-py>=1.0.0",
    "open-spiel>=1.2.0",
]

[tool.uv]
package = false
